<%= form_for(dailyreport) do |f| %>
  <% if dailyreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dailyreport.errors.count, "error") %> prohibited this dailyreport from being saved:</h2>

      <ul>
      <% dailyreport.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :report %>
    <%= f.text_field :report %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
